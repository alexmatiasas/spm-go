name: mutation
on: [push]
jobs:
  mutmut:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: astral-sh/setup-uv@v8
        with:
          enable-cache: true
      - run: uv sync
      - run: uvx mutmut run --paths-to-mutate src/ --tests-dir tests/
